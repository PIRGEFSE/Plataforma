# Dependencias del proyecto PIRGEFSE
# Sincronizado con .venv — Python 3.13.5
# Última actualización: 2026-05-25

# ── Backend API (FastAPI) ─────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Base de datos (ORM async + driver PostgreSQL)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.31.0

# Autenticación y seguridad
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
python-multipart==0.0.12

# Validación y configuración
pydantic==2.9.2
pydantic-settings==2.5.2

# Cliente HTTP
httpx==0.27.2

# ── Scripts de carga de datos (BBDD/load_dim_ive.py y similares) ──────────────
# Procesamiento de archivos Excel y datos tabulares
pandas==3.0.3
openpyxl==3.1.5

# Driver síncrono PostgreSQL (psycopg2) — usado en scripts de carga
psycopg2-binary==2.9.12
